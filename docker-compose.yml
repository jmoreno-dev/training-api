services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: training-api-db
      POSTGRES_USER: root
      POSTGRES_PASSWORD: Pass1234
    ports:
      - "5432:5432"
    volumes:
      - training_pg_data:/var/lib/postgresql/data

volumes:
  training_pg_data: